

enum TextSizes {
  small,
  medium,
  large,
  xlarge,}
  
enum OrderStatus {processing, shipped, delivered}


enum PaymentMethod {cash, paypal,googlePay,applePay,visa,mastercard,creditCard,paystack,razorpay, paytm, maya, gcash }